[project]
name = "hello"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["typing_extensions"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hello"]
